class startSubProgram {
	constructor(callName) {
		this.className = "StartSub";
		this.call = callName;
		this.lineNum = getCurrentLineNum();
	}
	
	action(j) {
		//console.log(this.endX + "   " + this.endY);
	}
}