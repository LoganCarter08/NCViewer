var origin = 0; // 0 = bottom left 
var scale = 15; // how zoomed in we start with
var currentColor = "black"; // color to set the line. black by default. 
var arcCenter = 1; // 1 for incremental, 0 for absolute
var punchToolAngle = 0;